#!/bin/bash
name=staggered

# Compile locally
bash ../make.sh $name

#bash ../make.sh $name sphinx publish

# Compile multiple formats and publish documents and soure code to INF5620
#bash ../make.sh $name sphinx publish src
